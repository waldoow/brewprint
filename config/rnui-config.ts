// React Native UI Lib Configuration
// Must be imported before any RNUI components

// Enable dark mode support for RNUI
require('react-native-ui-lib/config').setConfig({
  appScheme: 'default'
});

// Export to ensure this config is loaded
export {};